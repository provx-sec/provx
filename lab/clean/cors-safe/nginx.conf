# Clean CORS baseline for the `cors` adapter - the false-positive tripwire. It returns no
# cross-origin sharing headers at all, so no CORS check fires.
server {
    listen 80;
    server_name _;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
