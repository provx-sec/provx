# Clean disclosure baseline for the `wellknown` adapter - the false-positive tripwire. It
# serves a valid RFC 9116 /.well-known/security.txt (Contact + Expires), so no check fires.
server {
    listen 80;
    server_name _;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
