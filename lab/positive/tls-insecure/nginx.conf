# Deliberately-insecure transport baseline for the `tls` adapter. Serves over plain HTTP
# with no HSTS header and no HTTP->HTTPS redirect, so both transport-hygiene checks fire.
server {
    listen 80;
    server_name _;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
