# Deliberately missing every security response header - the positive accuracy case.
# Authorized, self-contained testing only; this target is never exposed off the lab network.
server {
    listen 80;
    server_name _;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
