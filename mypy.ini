# Root mypy config for the Provx monorepo CI `types` gate.
# Strict checking over the shippable Python source (tests are covered by the pytest gate).
[mypy]
python_version = 3.12
strict = True
# Resolve cross-package imports when mypy is pointed at the source dirs.
mypy_path = backend:packages/adapters/src
